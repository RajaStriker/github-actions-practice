name: Echo Messages Workflow

on:
  workflow_dispatch:   # manual trigger only

jobs:
  simple-echo:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello Striker 🚀 (manual run)"
